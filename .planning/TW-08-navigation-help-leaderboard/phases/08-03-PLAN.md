# 08-03 — Leaderboard Screen

## Objective
Display the top players in the galaxy, ranked by net worth.

## Context
- See `../BRIEF.md`
- See `../ROADMAP.md`
- `GET /api/leaderboard` already works and returns player data
- No screen renders this data yet

## Tasks
1. Create `LeaderboardScreen.tsx` component:
   - Fetch data from `cloudGetLeaderboard(galaxyId, 10)`
   - Display table: Rank | Ship Name | Class | Net Worth | Kills | Deaths
   - Highlight current player if in top 10
   - Handle loading and empty states
2. Add `leaderboard` to `AppMode` (or handle internally in WelcomeScreen)
3. Add menu item to WelcomeScreen: "🏆 Leaderboard"
4. Add keyboard shortcut in CloudSectorScreen: `L` for leaderboard
5. Handle `Esc` to return to previous screen

## Verification
- [ ] Leaderboard shows top 10 players
- [ ] Data matches `curl /api/leaderboard?galaxyId=1`
- [ ] Current player highlighted if ranked
- [ ] Empty state handled (no players yet)

## Done When
- Leaderboard accessible from menu and sector view
- Displays accurate rankings
- Keyboard navigation works

## Notes
- The API already returns `display_name`, `ship_name`, `class_id`, `net_worth`, `kills`, `deaths`
- For local mode, leaderboard could show saved game slots with net worth
- Consider adding K/D ratio column
